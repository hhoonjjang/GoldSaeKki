import styled from 'styled-components';

const ListComponent = () => {

    return (
        <ContentBox>
            하이
        </ContentBox>
    );
};

export default ListComponent;

const ContentBox = styled.div`
`;