import { Router } from "express";

const router = Router();

import db from "../models/index.js";

// router.post("/regist", (req, res) => {
//     tempuserId = req.body.userId;
//     tempuserPw = req.body.usepw;
//     tempuserName = req.body.userName;
// }).then(data)

export default router;
